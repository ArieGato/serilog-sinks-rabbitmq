<Project>

  <PropertyGroup>
    <LangVersion>latest</LangVersion>
    <GenerateAssemblyInfo>true</GenerateAssemblyInfo>
    <ImplicitUsings>enable</ImplicitUsings>
    <RepositoryType>git</RepositoryType>
    <DebugType>embedded</DebugType>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <!-- https://help.github.com/en/actions/configuring-and-managing-workflows/using-environment-variables -->
    <ContinuousIntegrationBuild Condition="'$(GITHUB_ACTIONS)' == 'true'">True</ContinuousIntegrationBuild>
    <CurrentYear>$([System.DateTime]::Now.ToString(yyyy))</CurrentYear>
    <Copyright>Copyright © Serilog Contributors 2019-$(CurrentYear)</Copyright>
    <PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <SignAssembly>true</SignAssembly>
    <PublicSign Condition=" '$(OS)' != 'Windows_NT' ">true</PublicSign>
    <AssemblyOriginatorKeyFile>../../assets/Serilog.snk</AssemblyOriginatorKeyFile>
    <Nullable>enable</Nullable>
    <MinVerMinimumMajorMinor>8.0</MinVerMinimumMajorMinor> <!-- next major version -->
    <MinVerTagPrefix>v</MinVerTagPrefix> <!-- version tags begin with 'v' -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <PropertyGroup Condition="$(MSBuildProjectName.Contains('.Tests'))">
    <NoWarn>$(NoWarn);CS1591;SA1600</NoWarn>
    <!--
      Keep absolute source paths in test assemblies. ContinuousIntegrationBuild rewrites them
      to /_/... and Shouldly's ShouldMatchApproved locates the .approved.txt file from the
      stack frame's source path. Shouldly reverses the mapping through the
      SHOULDLY_SOURCE_PATH_MAP environment variable, but the targets that set it only hook
      the VSTest target, which Microsoft.Testing.Platform never runs — so the approval test
      would fail on CI with "Unable to resolve source file from deterministic build source
      path". Test assemblies are not shipped, so deterministic paths buy us nothing here.
    -->
    <DeterministicSourcePaths>false</DeterministicSourcePaths>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="StyleCop.Analyzers" PrivateAssets="all" />
    <AdditionalFiles Include="$(SolutionDir)/stylecop.json" Visible="false" Link="stylecop.json" />
  </ItemGroup>

  <ItemGroup Condition="$(MSBuildProjectName.Contains('.Tests'))">
    <Using Include="Xunit" />
    <Using Include="System.Text" />
    <Using Include="Shouldly" />
    <Using Include="Serilog.Formatting.Json" />
    <Using Include="RabbitMQ.Client" />
    <Using Include="RabbitMQ.Client.Events" />
    <Using Include="RabbitMQ.Client.Exceptions" />
  </ItemGroup>

</Project>
